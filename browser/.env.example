### Copy this file to `.env` and fill in your real API keys.
### Do NOT commit your `.env` file. The repository-wide .gitignore already ignores `.env`.

# Gemini (Google Studio AI) API key
GEMINI_API_KEY=your_gemini_api_key_here

# Fish Audio API key
FISH_AUDIO_API_KEY=your_fish_audio_api_key_here

# Example additional settings
# GEMINI_MODEL=gemini-pro
# FISH_AUDIO_ENDPOINT=https://api.fishaudio.example/v1/process

# Usage: create a `.env` file next to this example with your real keys.
# Then start the app from the `browser/` folder: `npm run dev`.
